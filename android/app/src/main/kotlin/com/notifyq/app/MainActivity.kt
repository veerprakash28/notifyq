package com.notifyq.app

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // Future: Override onResume to reschedule notifications if needed
    // Future: Handle deep links from notification payloads
}
